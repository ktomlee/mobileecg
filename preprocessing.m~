%Pre-processing

ecg = load('1002603.txt');

N = (ecg);
fs = 500;
%t = (1:N);

%row, column
leadI = ecg((1:N),2);

plot(leadI);
%t = (0:N-1)/fs;

C=num2cell(N,1); %call st{1}, st{2} etc... up to 13 (st{1} is time counter)

i=1;
while i < 14
    plot(C{i});
    hold on
    i=i+1;
end
